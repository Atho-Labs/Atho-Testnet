[package]
name = "atho-gpu-native"
version = "0.1.0"
edition = "2021"
license = "Proprietary"

[features]
default = []
gpu-native = []

[dependencies]
atho-errors = { path = "../atho-errors" }
thiserror = "1"

[build-dependencies]
cc = "1"

[dev-dependencies]
atho-core = { path = "../atho-core" }
